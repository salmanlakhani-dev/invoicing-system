rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Restrict read and write access to authenticated users (the system owner) only.
    // Public queries are handled via secure backend operations (Cloud Functions/Server Actions).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
