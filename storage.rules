rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated system owners can read and write to Storage buckets.
    // Public download of invoice PDFs uses secure, short-lived signed URLs.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
